services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: dws-backend
    restart: unless-stopped
    env_file:
      - ./backend/.env
    environment:
      MONGODB_TLS: ${MONGODB_TLS:-false}
    ports:
      - "8000:8000"
